{
  "name": "fba-api",
  "version": "1.0.0",
  "description": "Node.js + Express + MongoDB API",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "deploy": "git add . && git commit -m 'deploy' || echo 'No changes' && git push && curl https://api.render.com/deploy/srv-d4il71gdl3ps73da7vfg?key=N3cBIIlnY-0",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
